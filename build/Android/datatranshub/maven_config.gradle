uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: 'http://nexus.luojilab.com/repository/igetapp/') {
                authentication(userName: 'luojilab', password: 'm6RzWncYBhMGPFtX')
            }
            pom.groupId = 'com.luojilab.dedao.base'
            pom.version = VERSION_NAME
            pom.artifactId = 'data_trans_hub'
        }
    }
}